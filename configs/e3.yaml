policy_path: "policy" 
xml_path: "e3/scene_terrain.xml"

simulation_duration: 600.0
simulation_dt: 0.002
control_decimation: 10

kps: [
      100, 80, 80, 100, 20, 20,
      100, 80, 80, 100, 20, 20,
      100,
      20, 20, 20, 20,
      20, 20, 20, 20,
      ]

kds: [
      4, 2, 2, 4, 0.7, 0.7,
      4, 2, 2, 4, 0.7, 0.7,
      4,
      0.5, 0.5, 0.5, 0.5,
      0.5, 0.5, 0.5, 0.5,
      ]

default_angles: [  
                  -0.10,  0.0,  0.0,  0.15, -0.0, 0.0,
                  -0.10,  0.0,  0.0,  0.15, -0.0, 0.0,
                  0.0,
                  0.05,  0.1,  0.1,  1.3,
                  0.05, -0.1, -0.1,  1.3,
                  ]

ang_vel_scale: 0.25
dof_pos_scale: 1.0
dof_vel_scale: 0.05
action_scale: 0.25
cmd_scale: [2.0, 2.0, 0.25]
num_actions: 21
num_obs: 72
include_phase_in_obs: False

cmd_init: [0., 0, 0]

# Input device settings (mutually exclusive - only one can be enabled)
gamepad_enabled: False  # Enable gamepad control (set to False if using keyboard)
gamepad_type: logitech  # Gamepad type (only used if gamepad_enabled is True)

keyboard_enabled: True  # Enable keyboard control (set to False if using gamepad)
cmd_step: 0.2  # Step size for velocity command changes (m/s or rad/s)
camera_angle_step: 0.02  # Step size for camera angle changes (radians)
camera_distance_step: 0.02  # Step size for camera distance changes (meters)
camera_elevation_step: 0.02  # Step size for camera elevation changes (radians)
